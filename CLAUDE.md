@AGENTS.md

## Claude-specific notes

- For patch or registry work, open the matching playbook under `project/skills/` before editing multiple crates.
- When changing node IDs or interfaces, verify against `schemas/patch.schema.json` and `fixtures/canonical_patch.json` in the same change set.

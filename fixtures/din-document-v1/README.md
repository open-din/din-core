# DinDocument v1 fixtures

Copied from `open-din/v2/examples` in the workspace parent for self-contained `din-core` CI.

Refresh when the normative examples change:

- `minimal.din.json`
- `orchestrated-scene.din.json`
- `invalid/invalid-default-scene.din.json` → `invalid-default-scene.din.json`
- `invalid/invalid-enum-value.din.json` → `invalid-enum-value.din.json`
